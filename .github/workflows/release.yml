name: release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    uses: platform-mesh/.github/.github/workflows/job-node-test.yml@9ddacafbe3260312cb1cc3f9974ad2fece8effe0 # main
    secrets: inherit

  create-version:
    uses: platform-mesh/.github/.github/workflows/job-create-version.yml@9ddacafbe3260312cb1cc3f9974ad2fece8effe0 # main
    secrets: inherit

  publish:
    needs: [create-version, build]
    uses: platform-mesh/.github/.github/workflows/job-node-publish.yml@9ddacafbe3260312cb1cc3f9974ad2fece8effe0 # main
    secrets: inherit
    with:
      version: ${{ needs.create-version.outputs.strictVersion }}
      publishFromDist: true
      environment: npmjs:@platform-mesh/portal-ui-lib
