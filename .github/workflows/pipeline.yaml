name: pipe
on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipe:
    uses: platform-mesh/.github/.github/workflows/pipeline-node-module.yml@07550bab80de7691f4947214ee342351896e209b # main
    secrets: inherit
    with:
      publishFromDist: true
      environment: npmjs:@platform-mesh/portal-ui-lib
