name: pipe
on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipe:
    uses: platform-mesh/.github/.github/workflows/pipeline-node-module.yml@be477afc04260ab8af16c4a2b7574bc6687b2cbd # main
    secrets: inherit
    with:
      publishFromDist: true
      environment: npmjs:@platform-mesh/portal-ui-lib
