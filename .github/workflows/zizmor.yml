name: Zizmor
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '30 4 * * 1'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  zizmor:
    uses: platform-mesh/.github/.github/workflows/job-zizmor.yml@33d3ee309a70b300c1775d78054cbb9215673b7b # main
    with:
      persona: 'auditor'
    permissions:
      security-events: write # Needed to write to the GitHub Security tab
      contents: read # Needed to read repository contents
      actions: read # Needed to read GitHub Actions workflows
